{
  "name": "mmscheduler-root",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-level ops tooling (migrations, seeding). The Next.js app lives in web/, the scraper in scraper/.",
  "scripts": {
    "install:all": "bun install && bun install --cwd web && bun install --cwd scraper",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "node db/migrate.mjs",
    "db:seed": "node scraper/steps/seed-from-json.mjs"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
