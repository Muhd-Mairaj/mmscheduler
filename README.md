# scheduler

To get started:
1. run `pip install -r requirements.txt` to download the required packages
2. place the excel sheet that will be parsed in the project folder
3. make sure that `excel_file_path` is correctly set in `"main.py"`
4. run `python main.py`

If you wish to create a virtualenv, please name it as `venv`
